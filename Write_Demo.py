# Library
import streamlit as st

# Creating Title
st.title("First App")

# Header
st.header("Why Streamlit?")
st.text("""Being able to build and publish data apps with less time consuming than other Frameworks is
a nice advantage, so it's useful to use.""")
 
# Subheader
st.subheader("Why You Need to Deploy?")
st.text("""Presenting your projects to users (clients) can provide useful feedback, 
and you can create a good portfolio by publishing these applications.""")
